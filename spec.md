# 足し算パズル — 要件・設計spec

## 1. 目的

暇つぶし用の、簡単な足し算パズル。作者本人が使う。

- 数字が並んだ盤面から、合計がお題の数になるように数字を繋げて消す
- 制限時間内にどれだけ消せるかを競う
- お題の数ごとに自己ベストを記録し、更新できたら嬉しい

これ以外の目的（学習効果、他人への提供、収益化）は持たない。判断に迷ったら「暇つぶしとして気持ちいいか」で決める。

## 2. 用語

| 用語 | 意味 |
|---|---|
| お題 | 合計の目標値。プレイ開始時に選び、セッション中は変わらない |
| 盤面 | 数字が並んだグリッド |
| セル | 盤面の1マス。1〜9のいずれかの数字を持つ |
| チェイン | プレイヤーがなぞって繋げた一連のセル |
| 成立 | チェインの合計がお題とちょうど一致すること |
| セッション | スタートからタイムアップまでの1プレイ |

## 3. ゲームルール

### 3.1 盤面

- 5列 × 6行 = 30セル
- 各セルは1〜9の整数を持つ
- 数字の出現は当面すべて等確率。詰まりやすい・簡単すぎるようなら後で重み付けを調整する（調整余地として残す）

### 3.2 操作

- セルを指で押さえ、繋げられるセルへなぞって伸ばしていく
- 繋げられるのは次のセル
  - 上下左右に**1マス**隣
  - 斜めに**1マスまたは2マス**。2マスの場合は間の斜めセルを飛び越える（間のセルは何でもよく、消費もしない）
  - 1セルから繋げられる候補は最大12セル
  - 繋げられるセルを事前にハイライトはしない。試して分かる形にする
- 同じセルは1チェイン内で1回まで
- チェインが自分自身と交差してもよい
- 直前のセルへ戻ると、そのセルがチェインから外れる（なぞり間違いの取り消し）
- チェインの現在合計を常に画面に表示する

### 3.3 成立と失敗

- **超過はNG、不足もNG、ぴったりのみOK**
- チェインの合計が**お題とちょうど一致**したら成立し、消える
  - 合計がお題を超えるセルへは繋げられない。なぞっても反応しない
  - 合計がお題未満のまま指を離した場合は不成立。チェインが解除されるだけで、ペナルティは無し
- ぴったり一致した時点で、そのチェインはそれ以上伸ばせない（どのセルを足しても超過するため）。**「ここで止めるか続けるか」の判断がプレイヤーに発生しない**
- 繋げられないセルをなぞろうとした場合、そのセルを短く震わせて知らせる（詳細は6.5）
- チェインは2セル以上必要（1セルでお題と一致しても成立しない）

### 3.4 補充

- 成立したセルが消えたあと、上のセルが落下する
- 空いた上端に新しい数字を補充する
- 落下・補充のアニメーションは短く（150〜200ms）。この間も制限時間は進み、操作も受け付ける

### 3.5 詰み対策

- 補充のたびに、盤面上にお題を成立させられる組み合わせが存在するか判定する
- 存在しない場合は盤面全体の数字を自動で組み直す（成立可能な配置を保証する）
- シャッフルボタンは置かない。プレイヤーが詰みを意識しなくて済む状態にする

### 3.6 時間とスコア

- 制限時間は**120秒固定**（お題ごとの自己ベストを比較可能にするため）
- スコア = **消したセルの総数**
  - 成立回数ではなくセル数にする。成立回数だと最小セル数の連打が唯一の正解になり、長いチェインを見つける楽しさが消えるため
  - 時間制限がある以上、長いチェインを探して粘るより、見つけた組み合わせを即座に消す方が時間あたりの効率は高くなりやすい。長いチェインは「狙うもの」ではなく「見つかったら嬉しいボーナス」として扱う
- 残り20秒を切ったら、残り時間表示で気づけるようにする（色の変化など。点滅はしない）

### 3.7 お題と自己ベスト

- お題の候補：**10 / 15 / 20 / 25**（初期セット）
- 自己ベストは**お題ごとに独立**して記録する
- 記録するのは最高スコアのみ（履歴は持たない）
- プレイ中、現在のベストを常時表示し、超えた瞬間にその場で分かるようにする
- セッション終了時、ベストを更新していたら結果画面で明示する

## 4. 画面

### 4.1 ホーム

- お題の一覧（10 / 15 / 20 / 25）を大きめのボタンで並べる
- 各ボタンに、そのお題の自己ベストを併記する（未プレイなら「—」）
- お題をタップするとそのままセッション開始（確認を挟まない）

### 4.2 プレイ

上から順に：

- お題の数（一番大きく表示）
- 残り時間 / 現在スコア / 自己ベスト
- 盤面
- チェインの現在合計（盤面の近く、指で隠れない位置）

中断はいつでも可能。中断したセッションのスコアは記録しない。

### 4.3 結果

- 今回のスコア
- 自己ベスト。更新した場合はその旨を明示
- 「もう一度」「ホームへ」の2択

## 5. データモデル

ローカル保存のみ。サーバーもアカウントも持たない。

```
BestScores: { [target: number]: number }
  例: { 10: 48, 15: 32, 20: 0 }

Settings: {
  theme: "dark" | "light" | "system"   // 既定は "dark"
}
```

保存先は端末のローカルストレージ。データが消えても致命的ではない前提で作る（マイグレーション機構は不要）。

## 6. デザイン要件

### 6.1 方針

超シンプル。装飾を足したくなったら足さない。情報は「お題・残り時間・スコア・ベスト・盤面・現在合計」だけ。

### 6.2 ダークモード（必須）

- **既定はダークモード**。ライトモードは用意するが優先度は低い
- 純黒（#000000）を背景に使わない。`#121212` 前後を基準にする
- 純白（#FFFFFF）をテキストに使わない。`#E0E0E0` 前後を基準にする
- 本文とのコントラスト比は 4.5:1 以上を確保する
- 彩度は抑える。強い原色をアクセントに使わない

### 6.3 目に優しくするための禁止事項

- 点滅・明滅する演出を作らない（ベスト更新演出も含む）
- 画面全体が急に明るくなる遷移を作らない
- アニメーションは 150〜200ms 程度に収める
- 効果音・バイブは既定でオフ

### 6.4 スマホ前提

- 縦持ち・片手操作を前提にする
- 盤面は親指の可動域に収まる位置に置く
- タップ/ドラッグ対象は最低 44pt 四方を確保する
- なぞり操作中に画面全体がスクロールしないようにする

### 6.5 フィードバック表現

繋げられるセルを事前に示さないぶん、フィードバックがルールを伝える唯一の手段になる。

**繋げられないセルをなぞろうとしたとき**

- そのセルを短く震わせる（100〜150ms、振幅は数px程度）
- 震えるのは該当セルのみ。画面全体や盤面全体は揺らさない
- 指がそのセルに乗り続けている間は繰り返さない。1回で止める
- バイブは伴わない
- 合計が超過するセル、繋げられない位置のセル、すでにチェインに入っているセル、いずれも同じ震えで扱う（理由による出し分けはしない）

**繋がったチェインの線**

- セル中心を結ぶ線で表示する。指の下でも見える太さにする
- 色は低彩度を1色だけ使う。背景から判別できるだけの明度差は確保する
- 発光、グラデーション、虹色変化、色の連続変化は使わない
- 合計がお題に近づいても色は変えない（数字表示で足りる）

## 7. スコープ外

以下は作らない。実装中に必要に思えても、まず本当に必要か確認する。

- アカウント、ログイン、クラウド同期
- オンラインランキング、他人との対戦、SNS共有
- 広告、課金
- 通知、リマインダー
- 連続プレイ日数、実績、バッジ
- プレイ履歴、統計グラフ
- 難易度設定（お題の選択以外）
- チュートリアル画面（ルールは触れば分かる範囲に収める）
- 多言語対応

## 8. 未決事項

実装前に決める。

1. **お題の候補**：10 / 15 / 20 / 25 で足りるか。実際に遊んでみて、25 が現実的に成立しにくいようなら差し替える
2. **盤面サイズ**：5×6 が片手でなぞれるか。窮屈なら 5×5 に減らす
3. **数字の出現分布**：等確率で始めて、お題が大きいときに詰まりやすいか確認する

## 9. 実装の進め方（提案）

実装形態は **PWA**。ブラウザで動かし、ホーム画面に追加して使う。これに伴う注意点：

- manifest で standalone 表示にし、アドレスバーが出ない状態で遊べるようにする
- セーフエリア（ノッチ・ホームインジケータ）を避けてレイアウトする
- なぞり操作は touch イベントで扱い、画面のスクロールとピンチズームを無効化する（6.4）
- オフラインで起動できるようにする（Service Worker でアセットをキャッシュ）
- iOS の Safari では、長期間使わないとローカルストレージが消えることがある。自己ベストが消えても動作に支障がない作りにしておく（5章の方針どおり）

手順：

1. ゲームロジックを画面から独立させて実装する（盤面生成・チェイン判定・成立判定・落下補充・詰み判定）
2. ロジックに対してテストを書く。特に「成立可能な組み合わせが存在するかの判定」はバグりやすいので優先する
3. その上に画面を載せる
4. 実際に数セッション遊び、未決事項（8章）を確定させてから数値を調整する

ロジックと表示を分けておくと、後でネイティブに作り直したくなっても中身を使い回せる。
